machine:
  services:
    - docker
  environment:
    WAIT_TRIES: 100
    WAIT_MS: 500
    _JAVA_OPTIONS: "-Xms248m -Xmx1024m"


test:
  pre:
    - docker-compose up -d

deployment:
  #production:
  #  tag: /release-.*/
  #  commands:
  #    - lein clean
  #    - lein uberjar
  #    - docker build -t mastodonc/witan.gateway .
  #    - docker tag -f mastodonc/witan.gateway mastodonc/witan.gateway:latest
  #    - docker tag -f mastodonc/witan.gateway mastodonc/witan.gateway:git-$(echo $CIRCLE_SHA1 | cut -c1-12)
  #    - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
  #    - docker push mastodonc/witan.gateway
  #    - ./scripts/deploy.sh $DEPLOY_IP production
  staging:
    branch: master
    commands:
      - lein clean
      - lein uberjar
      - docker build -t mastodonc/witan.gateway .
      - docker tag -f mastodonc/witan.gateway mastodonc/witan.gateway:latest
      - docker tag -f mastodonc/witan.gateway mastodonc/witan.gateway:git-$(echo $CIRCLE_SHA1 | cut -c1-12)
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push mastodonc/witan.gateway
      - ./scripts/deploy.sh sandpit-staging-deploy.mastodonc.net staging
